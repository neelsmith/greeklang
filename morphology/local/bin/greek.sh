# bash script to invoke groovy cli.
# default for binary is /usr/local/morphology/greek.a
# Options to support:
# -p --parser for alternate binary
# output options:
# -u -urns. URN output only
# -e --explain. Include second-tier urns
# -l --legible. Human-readable output
