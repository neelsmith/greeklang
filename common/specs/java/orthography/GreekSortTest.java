package orthography;


import edu.holycross.shot.greekutils.GreekComparator;

import org.concordion.integration.junit4.ConcordionRunner;
import org.junit.runner.RunWith;

@RunWith(ConcordionRunner.class)
public class GreekSortTest  {


/*
    public String greekSort(String s1, String s2) {
	int comparison;
	try {
	    comparison = GreekComparator.compare(s1, s2);
	    switch (comparison) {
	    case 0:
		return "equal";

	    case 1:
		return s2;

	    case -1:
		return s1;

	    default:
		System.err.println("Something went wrong sorting " + s1 + " and " + s2 + ".");
		break;
	    }
	} catch (Exception e) {
	    System.err.println("Failed to compare strings.");
	}
	return null;
    }
*/
}
